{
  "scripts": {
    "build": "cd node_modules/mp4box && npm i && npx grunt",
    "coverage": "nyc -a -c -r html -r text -r lcov npm test",
    "lint": "semistandard",
    "mocha": "mocha test  --recursive",
    "post-install": "npm run build",
    "test": "npm run lint && npm run mocha"
  },
  "dependencies": {
    "mp4box": "git+https://git@github.com/gpac/mp4box.js.git"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "nyc": "^13.0.1",
    "semistandard": "^12.0.1"
  },
  "main": "./index",
  "semistandard": {
    "env": [
      "mocha"
    ]
  }
}
