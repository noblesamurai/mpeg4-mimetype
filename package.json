{
  "scripts": {
    "lint": "semistandard",
    "build": "git submodule init && cd mp4box.js && npm i && npx grunt"
  },
  "dependencies": {},
  "devDependencies": {
    "semistandard": "^12.0.1"
  }
}
